import { type App } from "@vuepress/core";
import { type ThemeStatus } from "./status.js";
import { type ThemeData, type ThemeOptions } from "../../shared/index.js";
/**
 * Get client-side `themeData`
 */
export declare const getThemeData: (app: App, themeOptions: ThemeOptions, { enableBlog }: ThemeStatus) => ThemeData;
